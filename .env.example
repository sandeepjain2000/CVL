# Copy to .env and adjust paths. Relative paths are from the project root
# (CVL-ScraperLinkedIn_SendMails/).

# DeepSeek fallback for check_bounces.py (when NVIDIA NIM times out)
DEEPSEEK_KEY_FILE=../keysai/api_keys.json

# Optional overrides:
# DEEPSEEK_API_KEY=sk-...
# DEEPSEEK_MODEL=deepseek-chat
